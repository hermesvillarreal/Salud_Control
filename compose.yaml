services:
  db:
    image: postgres:16
    container_name: salud-control-db
    environment:
      POSTGRES_USER: salud_user
      POSTGRES_PASSWORD: salud_password
      POSTGRES_DB: salud_control
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  backend:
    build: ./backend
    container_name: salud-control-backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://salud_user:salud_password@db:5432/salud_control
      - TELEGRAM_BOT_TOKEN=your_fallback_token_here
    depends_on:
      - db
    volumes:
      - ./backend:/app
      - uploads_data:/app/uploads

  frontend:
    build: ./frontend
    container_name: salud-control-frontend
    ports:
      - "80:80"
    environment:
      - VITE_API_URL=http://localhost:8000
    depends_on:
      - backend

volumes:
  postgres_data:
  uploads_data:
